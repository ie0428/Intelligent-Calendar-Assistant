spring.application.name=Intelligent-Calendar-And-Conflict-Detection-Assistant

# ????AI ??
spring.ai.dashscope.api-key=${TONGYI_AI_KEY}
spring.ai.dashscope.chat.options.model=qwen3-max

# ???????
spring.datasource.url=jdbc:mysql://localhost:3306/intelligent_calendar_assistant?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=zhangjie6041
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# MyBatis??
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.ai.intelligentcalendarandconflictdetectionassistant.pojo
mybatis.configuration.map-underscore-to-camel-case=true

# JWT??
app.jwt.secret======================SmartCalendar======================
app.jwt.expiration.ms=86400000

# Redis配置
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.database=0
spring.data.redis.password=

# RAG向量存储配置
spring.ai.vector-store.redis.index=calendar-rag-index
spring.ai.vector-store.redis.prefix=rag:

# 嵌入模型配置
spring.ai.embedding.model=all-MiniLM-L6-v2

